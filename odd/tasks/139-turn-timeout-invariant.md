# #139 — Replace stale Redis dedup timeout invariant

## Contract
After #43/#44, `POST /webhook` commits inbound Meta IDs to PostgreSQL before acknowledging them. Redis dedup no longer guards the turn. A worker claims each row with a bounded lease; startup must ensure every WhatsApp runtime's **effective** turn timeout leaves at least 60 seconds within the 600-second default lease for lookup, reply persistence, provider send and completion. The worker still recovers expired claims after a crash; this guard prevents normal turns from predictably outliving their own lease. No change to Meta acknowledgement, provider send, or admission control.

## Scope
Isolated `/home/nh/wt-139-dedup`, branch `fix/139-turn-timeout-invariant`, base `origin/main` 098432e. Strict TDD. Remove the obsolete import-time Redis TTL assert; replace the per-runtime check with `effective_timeout + 60s < DEFAULT_LEASE_DURATION`. Test 300s accepted, 539s accepted, 540s rejected, partial/default effective limits. Delete unused `services/dedup.py` and its dedicated tests if no production callers; update the active root inventory `DOCUMENTACION.md` and EN/ES architecture docs. Leave historical delivery/SDD artifacts unchanged.

## Tasks
- [x] **T1 — RED and GREEN invariant.** Writer RED 4 failures: old TTL guard rejected 300/539 and lacked lease-boundary message; GREEN `pytest -q tests/test_main.py` 40 passed. Parent RED 1 failure for a one-day lease (`timedelta.seconds` wrapped to zero); GREEN 41 passed after `total_seconds()`. WhatsApp-only effective timeout + 60s < lease; 300/539 accepted, 540 rejected, partial/default effective limits and adapter-only exemption. Ruff check/format, mypy main and diff-check passed. Work-unit commit `e04d0bf16afcd4e10aee24491e337b4624c251ad`.
- [x] **T2 — Remove obsolete dedup and document.** Removed `services/dedup.py` and its dedicated tests after confirming no live Python callers. Updated both architecture languages and root active inventory/obsolete decision status without changing historical artifacts. Focused `test_main.py`: 41 passed; scoped Ruff check/format and diff-check passed. Work-unit commit `69bcf191abc94085cb1797aa29a6f082fc2dd86c`.
- [x] **T3 — Final verification and PR.** Full independent verification: `ruff check .` passed; `ruff format --check .` 132 formatted; `mypy src/` 55 files passed; `pytest -q` 1065 passed, 67 deselected, 18 xfailed (one non-failing audit-sink AsyncMock warning); `git diff origin/main...HEAD --check` passed. Independent read-only review found one P2 stale fail-open sentence in `DOCUMENTACION.md:53`; corrected, and the reviewer confirmed no remaining blockers. Native RDD inspect stopped at `managed_assets_outdated` and global sync was not authorized. Reviewer correction and verification evidence committed in `ca12b3229162ee59edc09427c9ec33146b62d6c7`. Pushed branch and opened [PR #173](https://github.com/nahuel893/agents-system/pull/173) directly against `main`, with `Closes #139`. Do not merge in this work unit.

## Rollback
Reverting the code restores the old import-time/startup restrictions and optional Redis service; no database migration or persistent data change. Do not force-push or touch the primary checkout.
