# Read-only SQL tool (#80)

Objective: ship a read-only, controlled SQL tool for agents whose model-authored SQL runs only inside database-enforced limits. Issue: #80 — Add a read-only, controlled SQL tool for agents.

Problem: agents can only run the 7 catalogued `run_report` reports; ad hoc read-only questions (live-test plan Phase 1) have no path. AD-2 forbids caller-built SQL, so the tool needs an explicit amendment and a new trust boundary: the database role, not application parsing.

Scope: new connector + application-layer query guard, a `query:sql` permission (new `Query` family), role verification, a provisioning script, offline + Postgres integration tests, CI wiring, ADR-007 (EN + ES) amending AD-2, tool docs (EN + ES). Out of scope: adding the tool to any predefined role (SemVer-relevant, separate decision), writes, DDL, multi-statement execution.

Constraints: strict TDD (RED before GREEN); public repo (no client names, emails, local paths, AI attribution); fixed error texts that never interpolate driver errors or connection details; every layer must hold on its own.

TDD: on (issue prompt: "Strict TDD"). Runner: `pytest` from the worktree root with the project virtualenv on `PATH` and the worktree `src` on `PYTHONPATH`.

Delivery strategy: single PR (explicit instruction). Forecast is well above the ~400-line heuristic because tests, bilingual ADR and the integration job travel with the behavior; reported in the PR body. Branch: `feat/80-readonly-sql-tool`.

Commit ids below are the ones after the rebase on `5a0cd8c`; every rebase rewrites them, the subjects stay the same. CI run ids in T5 name the pre-rebase heads they ran on.

- [x] T1 — Application-layer guard (`services/sql_guard.py`): parser-based single-SELECT validation, relation and function allowlists, canonical re-rendering, row-limit wrapping. Route: inline (executor). RED: collection ImportError (module missing). GREEN: 123 guard tests. Commit `a20cb10`.
- [x] T2 — Permission family `Query` + `query:sql` (T2) and the `sql_query` connector: read-only transaction, per-statement timeout, per-call role verification, N+1 fetch, JSON-safe cells, fixed error texts. Route: inline (executor). RED: collection ImportError. GREEN: 59 connector and role tests; full offline suite green. Execution lives in `services/sql_query.py` because connectors may not call `rollback()` (static contract test). Commit `620c5af`.
- [x] T3 — Database layer: `scripts/provision_sql_readonly.sql`, `verify_query_role`, Postgres integration test proving the role refuses writes with the application bypassed, CI wiring. RED: 34 failed / 1 passed before the role existed. GREEN: 45 passed on a throwaway PostgreSQL 16. Found and fixed: the planner evaluated `has_sequence_privilege` on a TOAST table, so privilege calls now sit behind CASE; provisioning made atomic. Commit `5ded4dc`.
- [x] T4 — ADR-007 (EN + ES) amending AD-2, tool docs (EN + ES), `reports.py` docstring pointer, live-test-plan gap update. Docs-only plus a docstring; offline suite green. Commit `0619e60`.
- [x] T4b — Live scenarios (`tests/test_live_eval_sql_query.py`) through a test-only role: an ad hoc question and a delete request. 3 runs each on a local `qwen2.5:3b` and on an OpenAI-compatible model: 100% pass, data intact. Commit `71ebde9`.
- [x] T4c — Self-review found an application-layer breakout: `E'\\'` rendered as `e'\'` let a later string become live SQL (confirmed on PostgreSQL 16; the role still refused it). Fixed: non-standard string literals refused, the rendering re-validated to a fixed point, `standard_conforming_strings` pinned. RED 7 failed / GREEN 131. Added a differential test: EXPLAIN of accepted text, as a role that could read the secret table, names only the allowlisted view's base table. Commits `d3728c0`, `11f5377`.
- [x] T5 — Full offline suite, lint, format, mypy; rebase; PR; CI. Reopened: CI run 36233987648 on `d29d818` was green, but run 36234157912 on `e86aebf` failed the T7c benchmark (joins, CTEs and scalar subqueries at the 1,000-token cap took 207 to 225 ms on the runner under coverage, budget 200 ms); fixed by T7h. Run 36234610294 on the T7h head was green (12 checks). After T7i and the rebase on `df18542`: run 36241454186 on `d45c055` green (12 checks).

- [x] T6 — Review fixes on PR #90 (route: inline executor, one commit per finding, strict TDD). Scratch PostgreSQL 16 with the fixture and provisioned role for RED/GREEN on the integration side.
  - T6a — Schema-qualified table functions in FROM (`SELECT * FROM public.lower('x')`) passed the guard. RED 11 failed; GREEN 147 guard tests. Commit `3f1233e`.
  - T6b — Guard CPU was quadratic (per-node renders; AND/OR are function nodes) and ran on the event loop: 400 terms 3.2 s, max length 30 s. Function names and types are now checked during the one render (fail-closed for any node it skips), 2,500-node budget, guard in `asyncio.to_thread`. RED 5 failed; GREEN 211; probe: 30.6 s call / 30.7 s stall became 0.0 s / 0.1 s. Commit `0bd4520`.
  - T6c — Role check ignored memberships (predefined `pg_*` roles), CREATE on the database and executable SECURITY DEFINER functions. RED 5 offline + 5 integration failed; GREEN 23 offline, 54 integration. Commit `c824869`.
  - T6d — Real connect failures (ConnectionRefusedError, socket.gaierror, asyncpg errors) escaped the connector and `verify_query_role`. RED 10 failed; GREEN 75 offline, 55 integration (closed-port case). Commit `d2d1964`.
  - T6e — Results were bounded in rows, not bytes (500 MB through one call). Byte gate in the database (running sum of row sizes, rows past `byte_limit` sent as NULLs) plus a JSON cut in the connector, `truncated_bytes`. RED 9 offline + 4 integration failed; GREEN 258 offline, 59 integration; probe: 100 x 5 MB rows now 0 rows, peak RSS 69 MiB (was 1,042 MiB). Commit `935bd5c`. Superseded by T7d: its window gate spilled to disk.

Acceptance: the #80 acceptance criteria. The live scenario runs through a test-only role and the direct runtime; running it through the HTTP API needs a predefined role that declares the tool and the Phase 0 harness (#78).

- [x] T7 — Review fixes on PR #90, round 2 (route: inline executor; the mapping needed four files already read in round 1, so no separate exploration; one commit per finding, strict TDD; scratch PostgreSQL 16 with the fixture and the provisioned role for the integration side; RED for the guard shown by running the new tests against the unfixed source tree).
  - T7a — CASE was always refused as `unparseable`: the fail-closed render check saw each WHEN branch (an If node written from inside the CASE) as an unchecked function. Found while building the benchmark. RED 4 failed; GREEN 173. Commit `2aa5ce1`.
  - T7b — (medium) `RecursionError` escaped `guard_query` (200 unary minus, 170 `::int`, 60 nested FROM subqueries) and aborted the turn. Every guard failure is now a fixed rejection. RED 6 failed; GREEN 182. Commit `effbe81`.
  - T7c — (high) sqlglot's parser doubles its work per bracket opened by a type keyword (nested `ARRAY[` depth 19: 33 s, then accepted); subscript chains are quadratic. Tokenize first and refuse >1,000 tokens (600 since T7h), >20 levels of bracket/CASE nesting (subscript chains count), >4 type-opened levels; parser node budget counting abandoned attempts; tree depth <= 80; guard on its own 4-thread executor. RED 10 failed against the unfixed guard (the two depth-40 shapes deselected: 2^40 on the old code) + 1 connector; GREEN 218 guard tests. Benchmark: the worst case at the caps was ~55 ms locally (see T7h), budget 200 ms; 107k random nested queries, worst 31 ms; reviewer probes rr4 (16 x depth-24 calls: all refused in 0.0 s, the next good call 0.0 s, no loop stall) and rr1 (worst 42 ms, nothing uncaught). ADR-007 corrected (EN + ES). Commit `bec1c4e`.
  - T7d — (medium + low) the byte gate's window kept whole rows in a tuplestore that spilled to disk (2 GB per call), and a column named `sql_query_rows` shadowed the whole-row reference. Per-row gate, cursor with `FETCH FORWARD 10`, running total in the connector, every FETCH on what is left of one statement timeout, row referenced as `"sql_query_rows".*`. RED 28 offline (fake rebuilt for the cursor protocol) + 4 integration (alias query_invalid; +55 MB temp_bytes; plan; statement timeout); GREEN 71 offline, 63 integration. Probe rr3 (3 x 100 rows of 20 MB): 0 temp bytes, 2.0 s, truncated_bytes (was 6 GB and a timeout). Commit `646477c`.
  - T7e — (medium, part 2) `temp_file_limit` was unset for the role. Provisioning sets it (default 256MB), `check_query_role` refuses unset or above 1 GB. RED: offline collection error (new constant) + 3 integration failed; GREEN 33 role tests, 66 integration. Commit `0d343b3`.
  - T7f — (low) ADR-003 EN + ES, the loader docstring and the built-ins test now list the `Query` family and `query:sql` (19 wire names). Docs and stale test only. Commit `3913eb5`.
  - T7g — (low) the re-review's unwrapped connect errors were already fixed by T6d; its probe on this branch returns `database_unavailable` / `None` for a closed port and a wrong password. Added the wrong-password integration case. Commit `9a30368`.
  - T7h — The T7c benchmark failed on a CI runner under coverage (about four times slower than locally): 207 to 225 ms at the 1,000-token cap. The guard no longer deep-copies each tree before rendering it and composes the final LIMIT wrapper as text (byte-identical output over a 54-query corpus); token cap 1,000 -> 600; best of five runs. Worst case now ~21 ms locally, ~65 ms under coverage locally. Commit `93a4c4c`.
  - T7i — (high, follow-through) An independent check of the T7c bound found shapes the benchmark missed: brackets opened by a type keyword side by side (`NULL[NULL[NULL[NULL[1]]]], ...`: 130 ms locally, 344 ms under coverage) or around a long list (60 ms / 207 ms), and subscripts side by side (`x[1], x[1], ...`: 55 ms; sqlglot re-analyses every subscript's index). All stayed under the token cap. The pre-scan now counts a token `2**k` times inside `k` type-opened brackets (against the 600 cap) and caps square brackets at 16. Route: inline (one source file, its test, the ADR twins). RED: 2 new unit tests failed against the unfixed guard, and under coverage 5 new benchmark shapes failed (206 to 344 ms). GREEN: 228 guard tests; slowest benchmark shape 26 ms locally, 76 ms under coverage; a scan of 108 type keywords x 4 levels x 2 shapes plus 100 sibling shapes, worst 25 ms; 9,382 random nested queries, worst 21 ms, nothing escaped. ADR-007 EN + ES. Commit `16dbc54`.
  - Local at T7i (then on `df18542`; `16dbc54` after the rebase on `5a0cd8c`): ruff check and format clean, mypy clean (74 files), 2064 passed / 17 xfailed, coverage 96.43% (gate 94), SQL integration 67 passed on a fresh PostgreSQL 16. Reviewer probes re-run: nested `ARRAY[` depth 17 to 40 refused in under 1 ms (was 7 to 33 s, accepted); rr4 16 x depth 24 all refused in 0.0 s, next good call 0.0 s, longest loop stall 0.05 s; rr1 worst 13 ms; rr3 1 and 3 concurrent 100 x 20 MB rows: 0 temp bytes, `truncated_bytes`; rr5 `sql_query_rows` alias keeps its own column; sec_p7 closed port and wrong password give `database_unavailable` / `None`.

- [x] T8 — Review fixes on PR #90, round 3 (route: inline executor; the four findings touch files already mapped in rounds 1 and 2, so no separate exploration; one commit per finding, strict TDD; a scratch PostgreSQL 16 in a 1 GB container with the fixture and the provisioned role for the integration side and the memory repro). Every reviewer probe was reproduced before its fix.
  - T8a — (medium) The pre-scan popped the innermost level on every END, but sqlglot reads an unquoted `end` as a column name: `ARRAY[end, ...` and `(end + ...` opened levels the scan forgot, undercounting the depth, type-nesting and `2**k` caps (probe: 126.8 ms; `(end + ` x38 accepted past the depth cap of 20). A bracket now closes only its own bracket and END only a CASE; any other END is `unparseable`, except right after AS (a column label, as in PostgreSQL). RED: 1 unit test (the parser ran on `SELECT end FROM sales_v`) and, under coverage, the new `end-filled` benchmark shape at 232 ms (budget 200). GREEN: 233 guard tests; the probe is refused in 0.1 ms; 92,350 random nested queries with `end`, `case` and `AS end` fillers, worst 15.2 ms, nothing escaped. Commit `2a04e54`.
  - T8b — (medium, reported twice) asyncpg raises a plain ValueError / OverflowError for a date or timestamp outside years 1 to 9999 or an interval of millions of years; SQLAlchemy does not wrap it, so it escaped the connector and aborted the turn. Now `value_out_of_range` (fixed text, suggests `::text`); any other exception `query_failed`; both log the error class only. RED: 4 offline + 5 integration failed. GREEN: 75 connector tests, 72 integration; reviewer probes p6 and probe_decode give fixed results; probe_decode_pool: the next call on the pool answers. Commit `427eb19`.
  - T8c — (medium) An allowlisted view only had to be relkind v/m. Without `security_barrier` the planner pushed the model's predicate below the view's join, and data_error vs rows became an oracle (probe p7b: hidden maximum 50.00 recovered in 17 calls; with the barrier set by hand, only the visible 45.00). `check_query_role` reads each plain view's reloptions and refuses one without `security_barrier`; provisioning refuses to grant one (checked by hand: refusal is atomic, a materialized view passes); the fixture view is a barrier view. RED: 2 offline + 2 integration failed. GREEN: 35 role tests, 74 integration; p7b now gets `role_not_read_only` from its first call. Commit `51b0e28`.
  - T8d — (high) PostgreSQL has no per-query memory limit. Reproduced in a 1 GB container: p4b (`string_agg` over `generate_series`, default config) and a function-free probe (`s || s` through 18 nested subqueries, 1,908 characters) were both OOM-killed in under 2 s, and the postmaster ended every session and ran crash recovery. The guard cannot bound memory, so it is documented as a deployment boundary (a host with `vm.overcommit_memory = 2` and no tighter cgroup limit, a dedicated replica, a small pool) in ADR-007 EN + ES, the tool docs and the provisioning script; the ADR residual that claimed "only time" is corrected, and the R4 note says an `untrusted_input` role also gets the database's availability. The default allowlist drops `rpad`, `lpad`, `format`, `string_agg`, `array_agg`, `json_agg`, `jsonb_agg` (large values from short input); `generate_series`, `unnest`, `string_to_array` and `replace` stay (bounded by rows or linear in input, or no worse than `||`). Integration tests that need the dropped functions run under an opted-in config. RED: 8 guard tests. GREEN: 242 guard tests, 76 integration; p4 and p4b are refused by the guard. Commit `6d5fc75`.
  - Local at `6d5fc75` on `5a0cd8c`: ruff check and format clean, mypy clean (74 files), 2183 passed / 17 xfailed, coverage 96.49% (gate 94), SQL integration 76 passed on the scratch PostgreSQL 16.

Progress / evidence: T1 to T8 done; T5 closed with the green CI run after T7i, and again after T8 and the rebase on `5a0cd8c`: run 36244667631 on `97391cc` green (12 checks). Next: review and merge (human decision).

Follow-ups noted, not in this PR: `run_report` and `role_is_read_only` have the same unwrapped-connect-error gap (pre-existing).
