# #170 — Demo-backed reference tools

## Contract
Provide opt-in reference backends for `catalog_search`, `client_lookup`, `run_report`, and `message_sender` without selecting them in the production registry by default. Reuse the single existing `demo/company` PostgreSQL dataset and the portable sales-report catalog; do not copy product/customer rows into Python or seed a parallel fixture. This is demonstration infrastructure for #52, not production integration. A message is only **recorded**, never sent, and unknown recipients never produce delivery claims.

## Context and design boundary
Isolated worktree `/home/nh/wt-170-refbackends`, `feat/170-reference-backends` from `origin/main` 8ae7939. Demo schema already has articles/customers/report views, but no phone or vector index. Derive a deterministic synthetic lookup phone from `padron_clientes.nro_cliente` in the reference query (documented; no new persisted data). Catalog search is explicitly lexical over demo article rows and reports no fabricated vector similarity. Reports reuse `connectors.sales_reports.CATALOG` and the existing read-only `build_report_tool_spec` rather than reimplementing SQL. Sender uses a recipient from the same demo customers and records an in-process intent only. No production registry wiring, new dataset, real provider send, or changes to `/home/nh/wt-169-eval`.

## Tasks
- [x] **T1 — RED→GREEN four reference backends.** Tests first: 4 corrected public-contract RED failures, then one ledger isolation RED failure. `ReferenceBackends` explicitly exposes four opt-in `ToolSpec`s: lexical demo catalog, canonical synthetic-phone lookup against existing customers, closed portable report catalog, and in-process-only recorded sender. Missing data returns no result, unbound engine fails closed, no production wiring. Further 94% floor RED: reference module 93.84%; added input/schema edge tests to reach 95.89%, 12 focused tests passing. Ruff check/format and mypy src passed. Work-unit commit `d6eacfa8600c3e80487864b82b6fba527e246795`.
- [x] **T2 — EN/ES documentation and demo proof.** Both platform reference guides list all eight backends and exact explicit wiring, synthetic lookup, lexical limits, read-only BI, and record-not-send semantics. Loaded existing demo SQL only into a freshly existence-checked disposable PostgreSQL database; with session read-only, smoke found 2 catalog matches, client 1, 3 status report rows, one recorded message and one unknown recipient. Drop verified by a separate absence query. Updated reference module introduction to reflect PG data dependency. Work-unit commit `6c38037d1e146c425627b5969335ad1307f77bac`.
- [x] **T3 — Gates, independent review, PR.** Final Ruff check/format and mypy src passed; `pytest -q` 1076 passed, 67 deselected, 18 xfailed (two non-failing warnings); full-project coverage 95.20% (≥94%) and changed reference module 95.89%. Independent read-only review found one P1 mismatch: `client_lookup` advertised a generic phone example its synthetic parser rejects. New RED test failed as expected, corrected schema to valid demo prefix in work-unit commit `00f4bc09bb094cb634f3d22ad99e88329332d4c0`; reviewer rechecked and found no blockers. Pushed feature branch and opened [PR #176](https://github.com/nahuel893/agents-system/pull/176) directly against `main` with `Closes #170`; not merged. Native RDD inspect stopped at `managed_assets_outdated` (no global sync).

## Rollback
Revert feature commits; no production wiring or database migration. Only isolated disposable demo data may be loaded. No force-push or writes to primary checkout or concurrent #169 worktree.
