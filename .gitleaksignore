# Gitleaks allowlist. Every entry here must be a real, historical finding that
# has already been triaged -- this is NOT a blanket allowlist. A new finding
# must be triaged before it is added: identify the secret, fix the code if it
# is still live, rotate the credential, then record the rotation here as the
# reason. See ADR-002 H.30 (docs/architecture/adr-002-agent-model-and-
# capabilities.md).
#
# Format: one gitleaks Fingerprint per line (`commit:file:rule-id:line`),
# preceded by a comment explaining why it is safe to ignore. Comment lines
# start with `#`; gitleaks skips them.

# Issue #78 Phase 0 Slice 2 (PR #96) -- the curl-auth-header rule matched a
# documentation EXAMPLE token in the GET /metrics curl example
# (`curl -H "Authorization: Bearer some-long-random-token" ...` / its ES
# twin), not a real credential -- there is nothing to rotate. The current
# file content (as of the follow-up commit right after these) already
# rewrites the example to reference $METRICS_API_KEY instead of a literal
# value, the same safe pattern docs/platform/demo-entrypoint.md uses for
# $ADAPTER_API_KEY; these two entries only silence the now-historical
# commit's own diff, which gitleaks re-scans on every PR regardless of
# later fixes.
24157a189645fc9fc080cd7900ce884f11c21017:docs/platform/observability.md:curl-auth-header:26
24157a189645fc9fc080cd7900ce884f11c21017:docs/platform_es/observability.md:curl-auth-header:29
